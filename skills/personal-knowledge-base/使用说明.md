# 个人知识库 1.2.1

请让 WorkBuddy 从发布者确认的 GitHub 仓库完整克隆并检出固定提交，阅读 `SKILL.md` 与 `references/installation-lifecycle.md`。不要直接从 ZIP 覆盖安装，不手改收据。

首次使用请告诉 Agent：资料文件夹在哪里、知识库叫什么、保存在哪里，以及允许处理的范围。产品资料的细分类由 Agent 阅读正文后完成。

已经建立的库用自然语言要求“更新我的知识库”。查询和创作直接提出任务；资料没有支持时应明确说明。需要看图而当前模型/工具不支持时，应提示切换能力并保留进度，不伪造复核。

代码、库、原件和 OCR 运行时分别保存。发布包没有用户资料或授权记录。首次安装、宿主发现、实际查找和最终问答仍须在目标环境验证。
