# 1.3.0 · 工作坊修复与长图、Excel整合

- 合并之前独立的工作坊修复及多格式功能，保留既有修复。
- 图片可直接由宿主看原分辨率区域、保存正文和表格、断点续接；无需OCR先通过。修正读稿会替代旧结果，撤销不会恢复旧稿。
- XLSX保留坐标、公式与缓存、合并/隐藏关系和常见显示格式；按需调用实际计算工具，原件不变。
- 修复新格式正文分类失败、透明图片文字丢失、公式结果格误覆盖等整合问题。
- 已有安装走正常更新与备份，保留资料库、原件及用户笔记，不删库重建。

[本版验收证据与边界](docs/RELEASE-1.3.0-ACCEPTANCE.md)。不把模拟测试当成实际看图，也不承诺未支持格式已经完整解析。

---

# 1.2.2-rc.1 工作坊修复候选（未发布）

当前发布结果及个人资料不随补丁进入仓库。仅使用合成回归。

修复当前视觉待办误计历史请求、20条静默截断、prepare缺少参数指引、macOS外部决定文件的系统tmp别名；超大图片返回输入限制而非诱导重装OCR。明确删除源文件仍可能保留知识库旧内容，以及DOCX/XLSX/图片/PPT实际能力。

没有实现自动长图表格分块/合并、普通图片视觉兜底、PPT解析或完整Office表格保真；没有改变OCR质量阈值、模型、原件、既有用户库或正式安装。PR修复范围不得写成全部工作坊问题已解决。

原公开发布审计记录仅适用于原发布，不证明这个新候选已实机验收。

---

# 1.2.1 · 正式仓库名称

仓库正式名称为 `qwbwj3/personal-knowledge-base`。安装来源校验和文档链接同步更新，旧名称的安装收据仍须经过原有提交与文件校验后才能继续使用。

本次不修改知识库、检索、分类和 OCR 功能，不改变用户资料。`v1.2.0` 标签及其历史内容保留，新安装使用 `v1.2.1`。

---

# 1.2.0 · 最终代码交付

## 已整合

Windows安装与进程管理整改、可复用的提取与维护、PDF目录规则及宿主视觉兜底、Agent正文分类与目录用途持久化、缺看图能力的用户提示，均整合为同一运行版本。

新资料的具体类别由Agent读正文并交回正式接口，不以标题或开头关键词决定。既有OCR与视觉复核协议保留，不降低质量阈值，不接入新的收费模型或向量服务。

## 交付清理

运行Python/脚本与辅助程序保持已整合源码的字节；本次只修改包版本与通用使用文档，删除当前交付树中的旧开发归档、实机报告、业务样本及会话/运行数据。第三方许可和空白模板保留。历史开发记录不属于本发布包，也未因当前树清理自动从原GitHub仓库擦除。

## 验证边界

整合前受影响Windows回归共406次执行通过，两个解释器/编码组合，每组203；涵盖正文分类和相关维护、来源、PDF、视觉与撤权流程。该证据来自合成判断的程序回归，不是实际宿主模型分类准确率评测，也不表示用户最终验收完成。

最终交付另做文件清单、可疑秘密、个人信息与代码身份检查，结果见PUBLICATION-AUDIT.json。复杂表格、完整大容量端到端、独立备份恢复及目标宿主安装占用/续装/自然发现等没有因打包自动成为已通过。
