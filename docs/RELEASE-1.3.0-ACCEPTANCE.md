# 1.3.0 整合验收

本版合并工作坊修复与图片/Excel实现，保留两条分支的提交历史。此记录区分程序回归、实际工具执行与尚未验证项目。

## 已完成

- 工作坊待办分页、历史请求误计、授权参数提示、macOS临时路径、超大OCR图片提示回归。
- 长图内部切区、断点继续、全图/表格完整性校验、结果保存、正常入库、纠正和撤销。
- 修复修正图片后旧内容仍可复用的问题；透明PNG白底合成；区域包丢失后可重新准备。
- Excel结构与格式、公式/缓存区分、范围读取、共享/数组公式保护、合并锚点、真实错误与普通文本区分。
- 修复图片/XLSX分类接口返回错误提取版本，导致按接口原样回传分类失败的问题。
- WorkBuddy 5.5.4 实际宿主暴露已存在目录的创建兼容问题：其目录代理忽略 exist_ok 并将 EEXIST 包装成 PermissionError。对已核验的真实目录省略重复创建；缺失目录仍走原宿主操作，保留链接拒绝、错误类型与权限保护。覆盖搜索缓存和安装管理目录。
- 本机97项合成回归通过。包含有明确标识的宿主观察/回执测试替身，不能据此声称真实模型正确识别所有资料。

## 实际工具与数据流程

1. Codex实际打开一张1100×2300合成长图的概览及两个原分辨率区域，记录正文和3×3表格；通过正常更新、正文分类、查询、完整原文读取及引用校验。尾部短语与4,560.75金额可从原图绑定的来源读回，原件哈希不变。
2. Codex现有JavaScript表格引擎实际导入合成XLSX，明确执行重新计算：旧缓存[1,null]更新为[300,103]；输入200得[600,203]，输入50得[150,53]。原件不变，结果保存为独立情景回执，不作为正式产品依据。
3. Python artifact_tool在本机不可用，真实返回engine_required，未假称内置Python引擎运行。宿主可使用自己的计算工具完成host任务。
4. 干净Git来源安装旧版，再用新提交走正常更新：59个既有资料/状态文件逐字节不变，旧代码备份保留。Skill升级与资料维护分开，不删库重建。升级后的正常资料更新还核对了独立个人笔记，内容哈希不变。

## 声明边界

本版不承诺任意长图、复杂保险表格都无需核对。PPT、旧Office、Excel嵌图/图表、DOCX完整版式仍见格式说明。业务原件、客户信息、私人库和本机详细日志不进入公开仓库。

Windows、macOS、Ubuntu 的 97 项回归与 142 文件完整性检查全部通过：[跨平台记录](https://github.com/qwbwj3/personal-knowledge-base/actions/runs/34763270984)。

## WorkBuddy 实际隔离使用验收

WorkBuddy 5.5.4 的正常受保护环境完成：可信候选升级到 1.3.0（requested_commit 与实际版本匹配）、重新读取已安装说明、原库继续更新、搜索索引 ready、3 份正文归类 pending=0、查询选材、完整原文读取、5 处引用核验，以及 Excel A2:G2 的空格、格式、公式与旧缓存读取。长图由宿主实际打开概览与两个原分辨率区块，查询读回第 2 年 1,700、尾部 4,560.75 及验证短语。三份合成原件哈希均未改变。

宿主首次缺少 Pillow/pypdf，通过其正常托管环境准备；未修改保护、PYTHONPATH、代码或安装收据。已存在目录的问题在原宿主复现并修复后复测通过，未以清空缓存作为最终修复方式。

该 WorkBuddy 会话没有现成可调用的重算引擎，真实返回 engine_required/executed=false；保留 host 计算任务，未伪造结果、未安装新计算引擎。实际重新计算已由上文 Codex 的现有 JavaScript 表格引擎另行验证。完整本地报告与回执不含在公开包内。

未纳入本次实际宿主端到端验收：真实业务 PDF/OCR 全链路、Obsidian 应用打开界面、任意复杂保险表格的公式语义。自动化回归与合成验收不代表这些项目已验证。
