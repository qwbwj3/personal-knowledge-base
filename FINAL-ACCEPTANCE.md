# 给 WorkBuddy：最终代码的隔离部署与人工验收

连续完成可执行的工作，不重新展开工程长测试，不自行修改核心代码。先确认实际公开仓库地址和发布者独立给出的完整提交；没有确认公开或地址不受当前安装器支持时，明确停止部署，不假称已经拉取。

## 1. 可信拉取与隔离安装

匿名完整克隆经确认的公开仓库，不浅克隆旧私有历史，不把ZIP改成伪造origin后安装。检出发布者给出的固定SHA，确认工作区干净，执行 `python -B tools/verify_release.py`。版本应为 `1.2.0`。

阅读Skill安装文档，使用一个**新的隔离安装目标**和实际将用于执行Skill的解释器，按正常安装器传入`--apply --target <新隔离目录> --expected-commit <完整SHA>`。记录真实active.commit、版本匹配和格式能力；pending不是成功。缺正常依赖按已有授权准备；不下载新模型替代健康OCR，不提权、不改宿主shim、不手改收据。

旧正式安装、真实业务库和原件保持不变。随后验证WorkBuddy是否真的发现并加载本次Skill。磁盘安装成功不等于宿主已加载；需要默认Skill切换或正常重启时说明影响并请用户确认，不能既要求“不切换”又声称自然发现已通过。

## 2. 接续真实资料的独立副本

从本机已有工作记录恢复原先授权的真实测试副本和隔离库，不从Git仓库找业务样本、不全盘扫描。发布包不包含这些资料。明确库名、state-home、KB ID与OCR_HOME；检查现有任务已结束。

已有隔离库可正常update；用户明确要求全新最终测试库时，在新state-home和资料独立副本上establish，不清空旧库。现有完整提取可按协议复用，不能承诺新建库无需重新提取。原件及副本哈希前后分别核对，禁止硬链接原件做可修改测试副本。

读取classification.pending_count；有普通待归类项时Agent实际分页读正文，按classification.md提交带原文依据的批量判断。没有关键词不等于无法分类；真正用途歧义才集中询问。不得伪造本人确认、重写原件或把自述非真实资料变成业务依据。

## 3. 视觉能力与进度

需视觉复核时使用本会话实际看图工具。没有能力就调用visual-review --vision-capability unavailable，告知受影响数量、切换模型或人工核对的办法，保留进度，不假装已看图、不重建库、不重装健康OCR。

## 4. 最小自检后交给用户

检查一份已归类资料能找到、分页读取并提供正确原文和出处；检查找不到依据时明确说明，不以无关候选填答案。这里只是连通性自检，不替用户做完整业务验收。

一次性报告公开仓库状态、固定源SHA、实际安装active.commit、宿主是否自然加载/重启验证、库标识、可读/有效/待分类/待用户决定/非权威/未收录各自数量。让用户继续问正常业务问题。未做项目明确标未验证。反馈到公开仓库只留脱敏摘要，原件、正文、完整本机路径、账号和日志留本地。
